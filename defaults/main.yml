---
# Пакеты, необходимые для установки Docker
docker_packages:
  - apt-transport-https
  - ca-certificates
  - curl
  - gnupg-agent
  - software-properties-common

# Параметры контейнера по умолчанию
container_image: geerlingguy/demo-flask-api:latest
container_name: demo-flask-api
container_port: 5000 